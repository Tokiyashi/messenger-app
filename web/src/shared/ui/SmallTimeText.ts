import { styled, Typography } from "@mui/material";

const SmallTimeText = styled(Typography)`
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  font-size: small;
`;

export default SmallTimeText;