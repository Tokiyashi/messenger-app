import {Box, styled} from "@mui/material";

const UserNameAndTime = styled(Box)`
  display: flex;
  flex: 1;
  align-items: center;
  justify-items: center;
  gap: 1rem;
`

export default UserNameAndTime;